#ifndef MODES
#define MODES

enum modes
{
    scale,
    tare,
    customBrew,
    brew,
    presets,
    selectPreset,
    configurePreset,
    coffeeWeight,
    brewRatio,
    brewDuration,
    startTimer,
    stopTimer,
};

#endif // MODES